import { Controller } from "@nestjs/common";

@Controller('api/vi1/appointments')
export class AppointmentController {

    // Get all
    // Get all (tri par date)
    // Get all (tri par client)

    // Get by id (systeme d'id du praticien)

    // Create

    // Update 

    // Delete
}